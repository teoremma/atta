TODO
- Replace fixed-k clustering with size-capped merges (e.g., use SciPy linkage and stop merges that would exceed `n_sequences`).
- Optionally add a heuristic sweep around k to minimize cluster size variance.
